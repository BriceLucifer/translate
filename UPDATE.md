# 性能分析与优化计划

> 2026-08-10 分析结论：**瓶颈 95% 在模型推理，不在桥接层 IO。暂不需要 Rust 重写。**

## 当前延迟拆解（实测）

一轮 partial 刷新（0.5s 周期）各环节耗时：

| 环节 | 耗时 | 占比 |
|---|---|---|
| WebSocket 收 PCM + 缓冲管理 | <1ms | ~0% |
| WAV 编码 + HTTP 发给 vLLM | ~2-5ms | ~1% |
| **Whisper 推理（GPU）** | **300-1000ms** | **~70%** |
| Hy-MT2 翻译（GPU，已异步） | ~200ms | ~25% |
| JSON 回传浏览器 | <1ms | ~0% |

Python 桥接层（asyncio + websockets + httpx）整个周期 IO 开销 **<10ms**。

## Rust 重写评估

**结论：为 IO 性能重写不划算。** Rust 能把 10ms 压到 1ms，在 500ms+ 的推理延迟面前无感知；模型推理始终由 vLLM / llama.cpp 完成，与桥接语言无关。

Rust 真正有价值的场景（未来要做桌面版/分发版时再考虑）：

- 单 exe 分发，无需 Python/uv 环境
- 桥接启动 ~0.5s → ~50ms（但启动大头是 vLLM 模型加载 30-60s，与语言无关）
- 内存占用 ~10MB vs ~100MB

## 已知问题（按优先级）

### P0 - Whisper 调用串行积压

partial 刷新每 0.5s 触发一次，但 whisper 推理要 0.3-1s。请求经 `asyncio.Lock` 串行排队，窗口长时推理时间超过刷新间隔 → 队列积压，字幕延迟越拖越大。

**方案**：允许"丢弃过期 partial"——上一个推理还没回来就不发新的；回来时若已有更新的音频，结果直接丢弃并重跑。

### P1 - partial 重转整个未提交窗口

partial 每次都重新转写从上次提交点到现在的全部音频（最长 10s），窗口越長推理越慢。

**方案**：partial 只看最近 4-5s 音频（已提交 segment 作为锚点不受影响），partial 推理可稳定在 ~300ms。

### P2 - 缺少各环节耗时量化

目前只有 COMMIT/partial 文本日志，没有每阶段耗时数据，优化靠猜。

**方案**：日志加 `whisper_ms` / `queue_wait_ms` / `mt_ms`，先量化再优化。

### P3 - 启动需三个终端

vLLM（WSL）+ llama-server + stt_server 分别手动启动。

**方案**：一键启动脚本（启动三个服务 + 健康检查 + 就绪提示）。

## 优化路线图

1. P2 加耗时日志（先量化）
2. P0 + P1 算法优化（纯 Python，预期延迟降 30-50%）
3. P3 启动脚本
4. （远期）桌面悬浮窗版 / 需要分发时再评估 Rust 重写
